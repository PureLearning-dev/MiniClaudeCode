> [参考文档](https://learn.shareai.run/zh/s08/)

# Context Compact

使用 agent 是一个无法预知需要执行多少次的循环，如果循环次数过多，会导致 context windows 爆炸，从而导致大模型出错。

为了防止这种情况，我们需要执行上下文压缩、去除不必要的数据、将多余数据存储到硬盘中等可以减轻上下文空间的操作。

可以实现一个三层防护，第一层为预处理管线，第二层为自动压缩决策，第三层为应急兜底，使用这三层可以让 context windows 尽可能利用起来。

将上下文经过这个压缩组件后发送给 LLM 即可。