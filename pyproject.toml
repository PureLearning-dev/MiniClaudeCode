[project]
name = "mini-claudecode"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "anthropic>=0.116.0",
    "httpx[socks]>=0.28.1",
    "python-dotenv>=1.2.2",
    "pyyaml>=6.0.3",
]
